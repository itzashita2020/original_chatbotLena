# Environment Variables Template
# Копируй этот файл в .env.local и заполни реальными значениями
# НИКОГДА не коммить .env или .env.local в git!

# ============================================
# Application
# ============================================

# Окружение: development | staging | production
NODE_ENV=development

# URL приложения
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# Database (Supabase / PostgreSQL)
# ============================================

# Supabase Project URL
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Supabase Anon Key (публичный ключ, безопасно для клиента)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Service Role Key (СЕКРЕТНЫЙ! Только для server-side)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Прямое подключение к PostgreSQL (опционально)
DATABASE_URL=postgresql://user:password@host:5432/database

# ============================================
# Authentication
# ============================================

# NextAuth Secret (для JWT токенов)
# Сгенерируй с помощью: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here

# NextAuth URL
NEXTAUTH_URL=http://localhost:3000

# OAuth Providers (если используются)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# ============================================
# API Keys
# ============================================

# OpenAI API (если используется)
OPENAI_API_KEY=sk-your_openai_api_key

# Stripe (если используется для платежей)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_key
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# SendGrid / Email Service (если используется)
SENDGRID_API_KEY=your_sendgrid_api_key
EMAIL_FROM=noreply@yourdomain.com

# Cloudinary / Image Upload (если используется)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ============================================
# Analytics & Monitoring
# ============================================

# Google Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry Error Tracking
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn
SENTRY_AUTH_TOKEN=your_sentry_auth_token

# Vercel Analytics (автоматически на Vercel)
# VERCEL_ANALYTICS_ID=auto

# ============================================
# Feature Flags
# ============================================

# Включить/выключить фичи
NEXT_PUBLIC_FEATURE_NEW_DASHBOARD=false
NEXT_PUBLIC_FEATURE_BETA_ACCESS=false

# ============================================
# Development Only
# ============================================

# Debug mode (только для development!)
DEBUG=false

# Показывать детальные ошибки (только для development!)
NEXT_PUBLIC_SHOW_ERRORS=true

# ============================================
# Security
# ============================================

# CORS разрешенные домены (через запятую)
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Rate Limiting (запросов в минуту)
RATE_LIMIT_PER_MINUTE=60

# ============================================
# ВАЖНО: Безопасность
# ============================================

# ❌ НИКОГДА не коммить этот файл с реальными значениями!
# ✅ Копируй в .env.local и заполняй там
# ✅ .env.local должен быть в .gitignore
# ✅ Используй разные ключи для dev/staging/production
# ✅ Ротируй ключи регулярно
# ✅ Не расшаривай секреты в Slack/Discord
# ✅ Используй secret management (Vercel Env, AWS Secrets Manager, etc)

# ============================================
# Как использовать:
# ============================================

# 1. Копируй файл:
#    cp .env.example .env.local

# 2. Заполни реальными значениями:
#    - Получи Supabase credentials на https://supabase.com
#    - Сгенерируй NEXTAUTH_SECRET: openssl rand -base64 32
#    - Получи API ключи от сервисов которые используешь

# 3. Проверь что .env.local в .gitignore:
#    grep ".env.local" .gitignore

# 4. Никогда не коммить .env файлы:
#    git status  # .env.local не должен показываться

# ============================================
# Проверка переменных
# ============================================

# В production убедись что все критичные переменные установлены:
# - DATABASE_URL или SUPABASE credentials
# - NEXTAUTH_SECRET
# - API keys для используемых сервисов

# Используй runtime проверку в src/lib/env.ts:
# if (!process.env.NEXTAUTH_SECRET) {
#   throw new Error('NEXTAUTH_SECRET is not set');
# }
